<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mrb.keepitgreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SurveyActivity" />
        <activity android:name=".MapOfTreesActivity" />
        <activity android:name=".Survey2Activity" />
        <activity android:name=".SurveyEndActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".SeeDetailsActivity" />
        <activity android:name=".MapTrackerActivity" />
        <activity android:name=".RedeemActivity"></activity>
    </application>

</manifest>